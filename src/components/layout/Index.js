import React from 'react';
import Tracks from '../tracks/Tracks';

const Index = () => {
    return (
        <React.Fragment>
            This is the index view
            <Tracks/>
        </React.Fragment>
    );
}

export default Index;
